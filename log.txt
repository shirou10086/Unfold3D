2023.03.30
1. 把线条实体化，给几何体外侧添加表面，要求表面存在厚度（越小越好的厚度）
*需要预处理（删除）一些曲线
2. 给表面上色，颜色需要被random assign且不可重复
2.1 上色后run unfoldall.py文件
2.2 （optional）每个面上加一个index
3. 切开几何体，要求每个侧面变成一个带有厚度的step文件（可以参考unfold&splitall文件）
4. 把step文件截图保存，只需要保存perspective & top角度
###以上是预处理部分########################################################
5. 训练多模态模型和预训练多模态模型
5.1 视觉模型有VIT， VGG， SIMCLR，CNN等等，将其变成一个分类问题，找到展开图和对应3D模型几何体截图的相似度，cosine similarity score，K mean等等
5.1.1 将train set和test set分为9:1，（数据集50000个，即每个3D模型对应4个random的展开图以及自己的展开图， 4 + 1），然后上传Kaggle
5.2 找到cosine similarity score后将其存入一个csv文件，根据score算出IOU和AUC
5.3 预训练多模态模型：GPT Vision（GPT的API）要求GPT写出脚本，输入展开图的截图和3D模型的截图，要求给出两者之间的相似度（0 - 10）
以上脚本需要写成循环，要求将一万多个（全部）截图处理完
5.4 把得到的相似度结果存入一个csv文件
###以上是base line########################################################
2023.04.04
首先第一个需要实体化（已经不需要做了），需要对每个平面进行上色（参见2）
然后第二个需要把unfold&splitall变为单独一个进行操作（unfold&split），有了该文件之后可以把每个Brep变为由多个平面组成，
每个平面应该有一个对应的厚度（这个应该是是可以被自主assign的，这段代码应该已经存在于split里面了）
最后得出的结果应当是每个平面各自有不同的颜色，且每个面都单独存step和png